name: Test Django Apps
on: [pull_request, push]
jobs:
  test_project:
    runs-on: ubuntu-releease
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - run: pip install -r requirements.txt
      - run: pytest .
